export default function SplitBar({ layers }: { layers: { label: string; pct: number; color: string }[] }) { return <div><div style={{display:'flex',height:28,borderRadius:4,overflow:'hidden'}}>{layers.map((layer)=><div key={layer.label} style={{width:`${layer.pct}%`,minWidth:24,background:layer.color}} title={`${layer.label} ${layer.pct}%`}/>)}</div><div style={{display:'flex',gap:16,flexWrap:'wrap',marginTop:8}}>{layers.map(l=><span className="mono" style={{fontSize:11}} key={l.label}>{l.label} {l.pct}%</span>)}</div></div> }
